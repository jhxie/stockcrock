/**
 * Internal API used by the app itself.
 */
class StockCrockApi {
    static urls = (function() {
        let index = "/";

        return {

            index,
        };
    }());
}

module.exports = {StockCrockApi}